<h1>List</h1>
<% if @user.list %>
  <p>
    <%= link_to @list.name, single_list_path %>
  <small><%= link_to "Delete", @list, method: :delete, data: { confirm: "Are you sure you want to delete this list?" } %></small>
</p>
<% else %>
Create your to do list!
<br/><br/>
<div>
  <%= render partial: 'lists/form', locals: { list: @list } %>
</div>
<% end %>